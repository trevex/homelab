apiVersion: v1
kind: ServiceAccount
metadata:
  name: vxlan
  namespace: kube-system
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: vxlan
  namespace: kube-system
  labels:
    tier: node
    app: vxlan
spec:
  selector:
    matchLabels:
      app: vxlan
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        tier: node
        app: vxlan
    spec:
      hostNetwork: true
      serviceAccountName: vxlan
      tolerations:
      - operator: Exists
        effect: NoSchedule
      containers:
      - name: vxlan
        image: ghcr.io/trevex/homelab@sha256:e31c8757d011cf11e7b58211df037ed027db6abf30e3dc8ecf7edb7b7e206a7c
        securityContext:
          privileged: true
        volumeMounts:
        - name: cnibin
          mountPath: /host/opt/cni/bin
      volumes:
        - name: cnibin
          hostPath:
            path: /opt/cni/bin

