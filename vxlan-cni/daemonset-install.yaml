apiVersion: v1
kind: ServiceAccount
metadata:
  name: vxlan
  namespace: kube-system
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: vxlan
  namespace: kube-system
  labels:
    tier: node
    app: vxlan
spec:
  selector:
    matchLabels:
      app: vxlan
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        tier: node
        app: vxlan
    spec:
      hostNetwork: true
      serviceAccountName: vxlan
      tolerations:
      - operator: Exists
        effect: NoSchedule
      containers:
      - name: vxlan
        image: ghcr.io/trevex/homelab@sha256:f2bd4cb6a9c89cf1fa1e3817898164d2069cf16bcd5f9d9aff49ddb11d943d8c
        securityContext:
          privileged: true
        volumeMounts:
        - name: cnibin
          mountPath: /host/opt/cni/bin
      volumes:
        - name: cnibin
          hostPath:
            path: /opt/cni/bin

